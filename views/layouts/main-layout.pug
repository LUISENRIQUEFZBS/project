<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title #{titulo}
        link(rel="stylesheet", href="/css/main.css")
        block styles
    body   
        header.main-header
            nav.main-header__nav
                ul.main-header__item-list
                    li.main-header__item
                        a(href="/", class=(path === '/' ? 'active' : '')) SAMSUNG
                    
                    if user
                        form.li.main-header__item(action="/usuario/logout", method="GET")
                            button(type="submit" style="background-color:transparent;border:none;color:white;font-size:inherit;  cursor:pointer;") Logout
                    else
                        li.main-header__item
                            a( href="/login", class=(path === '/login' ? 'active' : '')) Login
                    li.main-header__item
                        a( ) Shopping
                    li.main-header__item
                        a() Search
                    if user 
                        div.main-header__item(style="color:white;")=user.nombres
        block content