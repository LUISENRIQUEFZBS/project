<%- include('../includes/head.ejs') %>
</head>
<body>
    <%- include('../includes/navigation.ejs') %>
    <main class="centered">  
        <div class="product-container">
            <div class="product-info">
                <h1><%= producto.nombre %></h1>
            </div>
            <div class="product-image">
                <img src="<%= producto.urlImagen %>" alt="<%= producto.nombre %>" />
            </div>
            <div class="product-info">
                <h1><%= producto.nombre %></h1>
                <h2 class="price">$<%= producto.precio %></h2>
                <p><%= producto.descripcion %></p>
                <ul>
                    <li>Pantalla principal (interior): Plegable Dynamic AMOLED de 6.7 pulgadas, resolución FHD+ (2640 x 1080 píxeles).</li>
                    <li>Pantalla exterior (secundaria): Pantalla AMOLED de 3.4 pulgadas o más grande para notificaciones, controles rápidos y widgets.</li>
                    <li>Frecuencia de actualización: 120 Hz para una experiencia más fluida.</li>
                </ul>
                <div class="quantity-selector">
                    <label for="quantity">Cantidad:</label>
                    <input type="number" id="quantity" name="quantity" value="1" min="1" />
                </div>
                <form action="/carrito" method="post">
                    <button class="btn" type="submit">Añadir al carrito</button>
                </form>
            </div>
        </div>
    </main>
    <%- include('../includes/end.ejs') %>
</body>